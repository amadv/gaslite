# Agent Configuration

This file is generated by `create-agent.sh` using the persona templates in `/etc/agent-personas/`.

If you are reading this default content, the agent was created without specifying a persona.
Edit this file directly, or recreate the agent with a persona:

    create-agent.sh <username> --persona <name>

Run `ls /etc/agent-personas/` to see what personas are available.
